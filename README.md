# basket-case
Basket Case is a web-app I built with Vue.JS+Vuetify+Pinia and a Laravel backend. What is it: user builds a shopping list with item name, price, and quantity, sees a running total update live, sets a budget cap, and gets a visual warning when they go over. That's it. Every Vue concept from before still has a home, just smaller.
